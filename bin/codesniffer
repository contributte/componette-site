#!/usr/bin/env sh

DIR=${1:-"app tests"}

# n => Do not print warnings (shortcut for --warning-severity=0)
# s => Show sniff codes in all reports
# p => Show progress of the run
vendor/bin/phpcs --standard=ruleset.xml --extensions=php,phpt --encoding=utf-8 --colors -nsp ${DIR}
