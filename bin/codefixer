#!/bin/bash

DIR=${1:-"app tests"}

# n => Do not fix warnings (shortcut for --warning-severity=0)
vendor/bin/phpcbf --standard=ruleset.xml --extensions=php,phpt --encoding=utf-8 --no-patch -n ${DIR}
